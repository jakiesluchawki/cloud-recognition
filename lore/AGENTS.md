# Lore Directory

Project memory for Cloud Recognition.

Required skills:

- `lore-codex` for general Lore work.
- `lore-codex-git` before every commit.
- `lore-codex-tasks` before every task lifecycle change.

All Lore content is written in English. Active thinking belongs to tasks;
the wiki describes only the current implemented state.
